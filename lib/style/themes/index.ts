import { base } from "./base";
import yellow from './yellow';

import { ThemesList } from "./utils";

export const DEFAULT_THEME: string = 'base';
export const themes: ThemesList = {
    base,
    yellow
}